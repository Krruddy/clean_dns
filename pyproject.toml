[project]
name = "clean_dns"
version = "0.1.0"
description = ""
readme = "README.md"
dependencies = [
    "dnspython",
    "packaging",
    "pefile",
    "pywin32-ctypes",
    "setuptools",
    "pytest"
]
